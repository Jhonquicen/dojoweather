function eliminaCookie(){
    var elementoCookie=document.querySelector(".cookie");//document.queryselector coje el elemento "cookie"
    elementoCookie.remove();
}

function loading();{
    alert("loading weather report of "+city+" ...")
}